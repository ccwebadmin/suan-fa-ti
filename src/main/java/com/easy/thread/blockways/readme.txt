线程的阻塞的方式
1. 调用sleep()函数：让线程休眠一段指定的时间，等待后再醒来执行。
2. 调用wait()函数：使线程等待某个条件的成立。
3. 调用join()函数：将线程加入到当前线程中，等待该线程执行完毕后再继续执行。
4. 获取锁对象：当获取某个锁对象时，如果该锁对象已经被其他线程占用，则当前线程就会被阻塞，等待锁对象可用后再继续执行。
5. 同步队列：线程会将自己添加到同步队列中，等待其他线程将它唤醒后再继续执行。
6. 线程池：当线程池中的线程都在执行任务时，新的任务就会被阻塞，直到线程池中有空闲线程可以执行任务。
